var React = require("react");

var Countdown = React.createClass({
    render:function(){
        return(
            <div>The countdown will go here eventually.</div>
        );
    }

});

module.exports = Countdown;