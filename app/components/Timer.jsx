var React = require("react");

var Timer = React.createClass({
    render:function(){
        return(
            <div>The timer will go here eventually.</div>
        );
    }

});

module.exports = Timer;